* recover / detect swapped data (need swaped pcap files for testing)
* more than 0xffff snaplen (huge packets) (need oversized pcap files for testing)
* repair ascii-mode transfered pcap files (whole packets!)
